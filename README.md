# kafka-producer-using-abstracta

This is a `Kafka Producer` that leverages Ekahaa's data abstraction platform `Abstracta` (https://ekahaa.com) to ingest millions of records in seconds into a Kafka system.
